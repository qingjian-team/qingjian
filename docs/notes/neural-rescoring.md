# 字级 Transformer 重打分：评测（2026-09-08）

## 做了什么

- 训练：`tools/lm-train`（私有，不在仓库）在中文维基完整 dump + LCCC（12.7 亿 token、字表 19147）上训了两档字级 decoder：
  small 23M（ppl 21.7）、base 36M / 上下文 256（ppl 19.2），4090 上各约一小时。
- 推理：`crates/qingjian-neural`（candle），与训练脚本的 Python 打分对拍一致。M1 上 Metal 后端最快：空前文单条 6 ms，
  64 字前文 × 8 条 133 ms；CPU（Accelerate）约慢一倍，candle 自带 gemm 再慢一倍。按 token 算吞吐很低，是每层十几个小算子的调度开销在主导。
- 接法：Core `sentence::convert_paths` 出 Viterbi 前 6 条路径，`Engine` 按 `路径分 + λ·(神经分 − 静态二元分)` 重排，
  神经分只替换静态二元的判断，个人 n-gram、用户加分、敲错代价原样保留（早先试过 `(1 − λ)·路径分 + λ·神经分` 的线性混分，
  λ 0.5 时个人计数被稀释得厉害）。前文是本会话最近上屏的 64 个字符。只有 CLI `--neural` 接它。

## 回放数字（同一份冻结日志 4867 条，`--replay`）

| 配置 | 词首选 | 整句首选 |
|---|---|---|
| 基线（二元 + 个人 n-gram） | 86.0% | 89.5% |
| 线性混分 λ 0.5 | 87.5% | 83.9% |
| 线性混分 λ 0.25 | 87.0% | 87.4% |
| 线性混分 λ 0.25 + 4 nat 门槛 | 86.9% | 87.1% |
| 静态替换 λ 1.0（完全用神经分代替二元） | 85.3% | 82.2% |
| 静态替换 λ 0.5 | 86.3% | 86.7% |
| 静态替换 λ 0.5，base 模型 | 86.3% | 86.8% |
| 静态替换 λ 0.5，前文 0 | 86.1% | 86.2% |
| 静态替换 λ 0.25，前文 0 | 86.4% | 87.6% |
| **静态替换 λ 0.25，前文 64** | 86.5% | 88.4% |

- 门槛没用：被翻掉的路径本来分就接近，不是个人计数被稀释。
- 模型大小没用：base 与 small 一样，瓶颈不在模型。
- 前文有用（+0.8 个点），虽然回放里的前文是本会话上屏文本首尾相接、缺了全部原样上屏的英文与标点的碎片。
- λ 越小整句越接近基线、词的增益越小：词 +0.5、整句 −1.1 时大致打平。

## 明细说明回放这把尺子在整句上量不准

最好的一档（静态 λ 0.25 前文 64）逐条对账：变差 43 条、变好 45 条。变差里一大批是**模型对、日志错**：
`shurukuang` 日志选了 输入狂、模型给 输入框；`tiaoshi` 条是 → 调试；`gengkuai` 更会 → 更快；`jiaohu` 叫和 → 交互；
`siyou` 死有 → 私有；`zuoyong` 做用 → 作用；`hade` 哈的 → 好的。日志里「选了」的整句多半是当时按空格接受的基线自己的输出，
误选也照记，所以基线在整句上天然占便宜。真正模型错的：我的 → 沃德 ×4（云端学来的错读音用户词），资料 → 字了，才对 → 猜对。
变好的是 候选框 ×4、我之前 ×2、实际 ×2、不显示 ×2、增量、实现、下载、本地、目录、资源库、重写、进度 这类实词。

## 整句评测集上的数字（同一份冻结集 8322 句，`--eval-text data/eval/sentences.tsv`）

回放那把尺子有偏，当天晚上做了干净的整句评测（main `9d58fbb`，CLI `--eval-text`）：拿用户自己写的中文文本
（本仓库 `docs/` 加个人技术笔记）按标点切成 3 到 20 字的小句，按词库读音转成无分隔全拼，冷启动喂给引擎，看整句候选是不是原句；
上文是同段前面的真实文本末 64 字符，写进输入历史给重打分用。不上屏、不学习，与日志里选了什么无关。

| 配置（都是静态替换） | 整句首选 | 字准确率 | 查询平均 / 最慢 |
|---|---|---|---|
| 基线（二元） | 37.6% | 76.9% | 0.7 / 25 ms |
| small λ 0.25，前文 0 | 39.0% | 77.6% | 19 / 116 ms |
| small λ 0.5，前文 0 | 39.8% | 77.9% | 21 / 147 ms |
| small λ 0.25，前文 64 | 40.1% | 78.3% | 60 / 396 ms |
| base λ 0.25，前文 64 | 40.3% | 78.3% | 89 / 639 ms |
| small λ 0.5，前文 64 | 41.6% | 79.1% | 61 / 781 ms |
| **small λ 0.75，前文 64** | **41.9%** | **79.1%** | 67 / 817 ms |
| small λ 1.0，前文 64 | 41.5% | 78.9% | 73 / 483 ms |

- **过了门槛**：最好一档比基线高 4.3 个点，字准确率高 2.2 个点；连最便宜的一档（λ 0.5 不给前文，21 ms）也有 +2.2。
- λ 的方向与回放相反：干净集上 0.5 到 1.0 基本一样好，0.75 最高；回放里 λ 越大整句越差，是因为回放的整句「正确答案」本来就是基线自己的输出。
- 前文值 1 到 2 个点（λ 0.5 时 39.8 → 41.6）。模型大小还是没用（base 与 small 打平，还慢一半）。
- 绝对数字低（基线 37.6%）是文本的性质：技术笔记与本仓库的设计文档，行话密（候选 / 词库 / 释义表 / 敲错），
  `docs/` 那部分基线只有 27.8%，个人笔记 46.5%。这个集是冷启动、没有个人 n-gram；实际使用里个人计数会补掉一部分同样的错，
  模型的净增益会比这里小一些。
- 延迟：这里是 6 条路径、句长 3 到 20 字，前文 64 时平均 60 到 70 ms，20 字长句最慢到 800 ms，仍然远超每键 10 ms 预算，要走停顿后异步重排。

λ 0.5 前文 64 与基线逐句对账：**翻好 382 句，翻坏 54 句**。翻好的是实词选择：同因此 → 同音词、京太累 → 静态类、
日至 → 日志、就错 → 纠错、蜜月 → 密钥、美剧 → 枚举、潜逃 → 嵌套、案件 → 按键、挡子弹 → 当字典、遥控、缴费、导向。
翻坏的多半是切句后失去了上下文的短句里的虚词 / 单字同音（是 / 时 / 先、会 / 回、见 / 建、拿 / 那、该 / 改），
还有 全绿 → 全旅 ×3、服务商 → 服务上 ×2、技术面试 → 及书面是 这类模型没见过的搭配。

## 结论与下一步

模型有信息、接法成立，**在不带日志偏差的尺子上过了「整句 +2 个点」的门槛**（+4.3）。回放上看到的「整句变差」是尺子的问题。下一步按顺序：

1. 延迟：前文 KV 缓存（前文在一次组句里不变，只算候选那几个字）、算子合并或量化；然后做成停顿后异步重排，壳才能接。
   没前文的一档 20 ms、+2.2 个点，是不做缓存也能先进壳的保底方案。
2. 应用上下文：把 IMK 的 surrounding text 喂给重打分（现在只有本会话历史），这是模型相对二元的最大理论优势；评测集里的「上文」就是它的模拟。
3. 数据：语料里没有技术文本，`输入框`、`候选框`、`全绿`、`服务商` 这类词靠个人微调补；闲时训练那一步才是「越用越像自己」的来源。
4. λ 缺省已从 0.25 提到 0.5（0.75 最高但与 0.5 只差 0.3 个点，留点余量给个人 n-gram）。
